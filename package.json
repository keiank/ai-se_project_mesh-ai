{
    "name": "meshai-fullstack",
    "scripts": {
        "install:all": "npm install && npm install --prefix server && npm install --prefix client",
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
    },
    "devDependencies": {
        "@types/morgan": "^1.9.10",
        "concurrently": "^10.0.3"
    },
    "dependencies": {
        "express-rate-limit": "^8.6.1",
        "morgan": "^1.11.0",
        "winston": "^3.19.0"
    }
}
